"""
cost-leakage-detector — Source package.

Modules:
    data_generator  — Synthetic procurement transaction dataset
    detector        — Multi-rule anomaly detection engine
    scorer          — Severity scoring (Low / Medium / High / Critical)
    reporter        — Excel report + summary statistics
    dashboard       — Interactive Plotly HTML dashboard
    alerter         — Slack webhook alerting for Critical findings
"""
